# 🧪 AOI-Vision Inspection 앱 기능 관통테스트(E2E) 시나리오 명세서

> **문서 버전**: `v1.4.0-TEST`  
> **최종 작성일**: 2026-08-20  
> **대상 시스템**: AOI-Vision Inspection (스마트 VLM 비전 검사 시스템)  
> **테스트 목적**: 하드웨어 제어, 광학 비전 분석, 딥러닝/VLM 판정, 오디오 신호, 능동 학습(Active Learning), 파레토 통계 분석 등 **시스템 전 기능의 유기적 상호작용 및 정합성을 검증하는 관통(End-to-End) 테스트**를 수행합니다.

---

## 📑 관통테스트 시나리오 매트릭스 (Test Execution Matrix)

| Phase | 테스트 시나리오 ID | 시나리오 명칭 | 핵심 검증 항목 | 대상 모듈 | 합격 기준 |
| :---: | :--- | :--- | :--- | :--- | :---: |
| **01** | `TC-SYS-01` | **엔진 초기화 & 라이브러리 적재** | OpenCV.js, TensorFlow.js, MobileNet 로드 | `App.tsx` | 로더 메시지 해제 및 준비 완료 |
| **02** | `TC-MST-02` | **골든 마스터(Golden Master) 등록 & ROI** | 마스터 캡처/업로드, ROI 사각/윤곽선 추출 | `LeftPanel`, `Modals` | 마스터 슬롯 저장 및 임베딩 완료 |
| **03** | `TC-CAM-03` | **카메라 제어 & 광학/디지털 듀얼 줌** | 회전, 반전, HW 광학 줌, SW 디지털 줌, 그리드 | `LeftPanel` | 왜곡 없는 줌 배율 및 화각 변환 |
| **04** | `TC-BAR-04` | **바코드/QR 코드 자동 인식 & 시리얼** | 1D Code128, 2D QR Code 실시간 스캔 매핑 | `barcodeScanner` | 시리얼/바코드 100% 자동 채번 |
| **05** | `TC-CFG-05` | **검사 감도 프리셋 & 파라미터 튜닝** | 정밀(Precision), 표준, 고속 프리셋 전환 | `CenterPanel` | SSIM, 이진화 임계치 실시간 튜닝 |
| **06** | `TC-INS-06` | **실시간 비전 판정 & 무지연 오디오 알람** | OK/NG 판정, 유사도 히트맵, 0ms 사운드 | `CenterPanel`, `audioAlert` | 0ms 무지연 OK/NG 합성음 출력 |
| **07** | `TC-FDB-07` | **실시간 결함 스냅샷 & 피드백 수집** | NG 발생 시 불량 유형 분류, 메모, 심각도 | `DefectFeedbackModal` | 결함 DB 즉시 적재 및 스냅샷 저장 |
| **08** | `TC-LRN-08` | **불량 지식베이스 & AI 능동 자가학습** | In-Context Few-Shot 프롬프트 생성, JSON 백업 | `DefectKnowledgeBaseModal`, `defectLearner` | AI 프롬프트 규칙에 현장 불량 100% 반영 |
| **09** | `TC-PAR-09` | **파레토(Pareto 80/20) 불량 분석 대시보드** | 빈도 막대차트, 누적 곡선, CSV 내보내기 | `DefectParetoModal` | 80/20 임계치 선 및 상위 결함 도출 |
| **10** | `TC-INT-10` | **스마트폰 무선 브리지 & 독립형 배포** | WebRTC 원격 노드, Standalone HTML 단일 패키징 | `SmartphoneBridgeModal`, `standaloneExporter` | 원격 프레임 전송 및 단독 파일 오프라인 구동 |

---

## 🔍 상세 테스트 시나리오 절차 (Step-by-Step Scenarios)

```
[시나리오 전체 워크플로우]
[Phase 1] 시스템 기동 ➔ [Phase 2] 기준 마스터 등록 ➔ [Phase 3] 카메라 줌/화각 튜닝
       ➔ [Phase 4] 바코드 스캔 ➔ [Phase 5] 감도 프리셋 ➔ [Phase 6] 실시간 비전 판정 & 음향
       ➔ [Phase 7] 결함 피드백 ➔ [Phase 8] AI Few-Shot 자가학습 ➔ [Phase 9] 파레토 분석
       ➔ [Phase 10] 스마트폰 브리지 / Standalone 내보내기
```

---

### 🔹 Phase 01: 엔진 초기화 및 디바이스 연동 (`TC-SYS-01`)
1. **사전 조건**: Chrome/Edge 최신 브라우저 실행, 웹캠(카메라) 디바이스 연결.
2. **실행 절차**:
   - 애플리케이션 접속 (`/` 또는 GitHub Pages).
   - 상단 로딩 스피너 및 `OpenCV.js 및 AI 라이브러리 엔진 적재 중...` 표시 확인.
3. **입력 데이터**: 기본 웹 환경.
4. **기대 결과**:
   - `cv`, `tf`, `mobilenet` 엔진이 정상 적재되고 2초 이내에 메인 UI로 전환.
   - 카메라 디바이스 목록이 좌측 패널 드롭다운에 자동 탐색되어 표시됨.
5. **합격 기준**: 엔진 오류 메시지 없이 메인 대시보드 상태가 `정상 대기`로 표시될 것.

---

### 🔹 Phase 02: 골든 마스터(Golden Master) 등록 및 ROI 추출 (`TC-MST-02`)
1. **사전 조건**: Phase 01 완료, 카메라 스트림 또는 샘플 이미지 준비.
2. **실행 절차**:
   - 좌측 패널에서 **[기준 마스터 캡처/등록]** 또는 `샘플 골든 마스터` 로드.
   - 검사 영역(ROI) 모드를 `직사각형(Rect)` 또는 `자동 윤곽선(Auto Contour)`으로 설정.
   - 마스터 저장 모달에서 마스터 명칭 `MASTER-PCB-REV2.4` 입력 후 저장.
3. **입력 데이터**: `SAMPLE_GOLDEN_MASTER_SVG` (표준 정상 PCB 이미지).
4. **기대 결과**:
   - 좌측 패널 `기준 마스터 프리뷰`에 캡처된 기준 영상이 렌더링됨.
   - 딥러닝 임베딩 벡터(`dlEmbedding`) 추출 완료.
5. **합격 기준**: ROI 영역 바운딩 박스가 정확하게 활성화되고 검사 준비 상태가 `MASTER OK`로 변경될 것.

---

### 🔹 Phase 03: 카메라 제어 & 듀얼 줌(광학/디지털) (`TC-CAM-03`)
1. **사전 조건**: 카메라 라이브 스트림 활성화.
2. **실행 절차**:
   - 좌측 패널에서 화면 회전 버튼 (0° ➔ 90° ➔ 180° ➔ 270°) 순차 클릭.
   - 좌우 반전(Flip) 토글 스위치 클릭.
   - 줌 컨트롤 슬라이더를 `1.0x`에서 `3.0x`로 조정 (하드웨어 광학 줌 지원 시 HW 줌 제어, 미지원 시 SW 디지털 줌 캔버스 스케일링 작동).
   - 그리드 라인(Grid) 토글 ON/OFF.
3. **기대 결과**:
   - 프레임 지연(Lag) 없이 실시간 캔버스 회전/반전/확대 축소 반영.
4. **합격 기준**: 줌 배율 변경 시 초점 영역 중앙 정렬 유지 및 FPS 25 이상 유지.

---

### 🔹 Phase 04: 바코드/QR 자동 스캔 및 시리얼 채번 (`TC-BAR-04`)
1. **사전 조건**: 바코드(Code128) 또는 QR 코드가 포함된 검사 대상 프레임 배치.
2. **실행 절차**:
   - 화면에 `AOI-PCB-M04` 바코드 제시.
   - 시스템의 자동 바코드 리더 엔진 작동 확인.
3. **입력 데이터**: `AOI-PCB-M01` ~ `AOI-PCB-M30` (참조 데이터셋 매핑 테이블).
4. **기대 결과**:
   - 상단 헤더 및 검사 정보 패널의 `바코드`와 `시리얼` 입력창에 `AOI-PCB-M04` / `SN-2026-PCB-004`가 0.5초 이내에 자동 입력됨.
5. **합격 기준**: 바코드 인식 실패율 0% 및 수동 타이핑 없이 검사 연동.

---

### 🔹 Phase 05: 검사 감도 프리셋 & 파라미터 튜닝 (`TC-CFG-05`)
1. **사전 조건**: 마스터 등록 완료.
2. **실행 절차**:
   - 중앙 패널 상단의 프리셋 버튼 순차 클릭:
     - `정밀(Precision)`: 임계치 97%, 이진화 35, 딥러닝 ON, VLM ON.
     - `표준(Standard)`: 임계치 95%, 이진화 50, 딥러닝 OFF, VLM ON.
     - `고속(Speed)`: 임계치 90%, 이진화 65, 초고속 OpenCV 단독 구동.
   - 슬라이더를 수동 조작하여 `사용자 지정(Custom)` 모드 전환 확인.
3. **기대 결과**:
   - 원클릭으로 최적 파라미터가 실시간 적용되며 즉각적으로 비전 연산에 반영됨.
4. **합격 기준**: 프리셋 전환 시 연산 중단이나 프레임 끊김 없이 파라미터 갱신.

---

### 🔹 Phase 06: 실시간 비전 판정 & 0ms 오디오 알람 (`TC-INS-06`)
1. **사전 조건**: 정상/결함 샘플 프레임 입력.
2. **실행 절차**:
   - **Case A (정상 검사)**: 정상 골든 샘플 투입 ➔ `검사 시작` ➔ 결과 `OK` 판정 및 고음 아르페지오(C5-E5-G5-C6) 무지연 사운드 출력 확인.
   - **Case B (결함 검사)**: 납 브릿지 결함 샘플(`SN-2026-PCB-004`) 투입 ➔ 유사도 74.2% 저하 ➔ 빨간색 `NG` 배지 및 저음 경고음(F3-D3) 출력 확인.
   - 사운드 토글 버튼(🔊/🔇) 클릭하여 음소거 제어 확인.
3. **기대 결과**:
   - 화면에 유사도 점수, 차분 히트맵, 결함 바운딩 박스가 렌더링되며 Web Audio API를 통해 0ms 즉각 음향 출력.
4. **합격 기준**: 판정 결과와 음향 피드백 간의 시간 오차 50ms 미만.

---

### 🔹 Phase 07: 결함 스냅샷 & 작업자 피드백 등록 (`TC-FDB-07`)
1. **사전 조건**: Phase 06에서 NG 판정 발생.
2. **실행 절차**:
   - NG 발생 시 화면에 표시되는 **[불량 피드백 & DB 등록]** 버튼 클릭.
   - 결함 유형을 `납 브릿지 (Solder Bridge)`로 지정, 심각도 `CRITICAL` 선택.
   - 작업자 보정 메모: `U1 MCU 14~15번 핀 솔더 브릿지 쇼트 확인. 인두기 리워크 조치` 입력 후 **[불량 DB 저장]** 클릭.
3. **기대 결과**:
   - 결함 스냅샷(Crop ROI)과 메타데이터가 `DefectRecord` 형식으로 로컬 지식베이스에 안전하게 저장됨.
4. **합격 기준**: 저장 후 하단 검사 로그 및 지식베이스 모달에서 즉시 조회 가능할 것.

---

### 🔹 Phase 08: 불량 지식베이스 & AI 능동 자가학습 (`TC-LRN-08`)
1. **사전 조건**: 누적된 불량 DB 레코드 5건 이상 존재.
2. **실행 절차**:
   - 상단 헤더의 **[🧠 불량 지식베이스 & AI 학습 센터]** 버튼 클릭.
   - 누적된 결함 리스트 및 유형별 통계 확인.
   - **[🧠 AI 불량 지식 학습 (Active Learning)]** 버튼 클릭.
   - VLM 프롬프트에 `[⚡ 현장 불량유형 DB 피드백 학습 규칙]`이 Few-Shot In-Context로 자동 생성 및 주입되었는지 확인.
   - **[JSON 백업]** 버튼 클릭하여 DB 파일 다운로드 확인.
3. **기대 결과**:
   - 작업자가 등록한 실제 결함 사례가 VLM 추론 프롬프트에 자동 반영되어 유사 결함 판정 정확도가 비약적으로 상승.
4. **합격 기준**: Few-Shot 프롬프트 규칙이 누락 없이 생성되고 학습 상태 배지가 `100%`로 갱신될 것.

---

### 🔹 Phase 09: 파레토(Pareto 80/20) 불량 원인 분석 대시보드 (`TC-PAR-09`)
1. **사전 조건**: 20건 이상의 검사 이력 데이터 존재.
2. **실행 절차**:
   - 상단 헤더의 **[📊 파레토 80/20 분석]** 버튼 클릭.
   - 불량 유형별 발생 빈도 내림차순 막대 그래프 확인.
   - 누적 점유율 곡선(Cumulative Line) 및 80% 임계 가이드선(80/20 Rule) 확인.
   - 상위 20% 핵심 원인(Vital Few) 요약 카드 확인.
   - **[CSV 내보내기]** 버튼 클릭하여 리포트 파일 생성 확인.
3. **기대 결과**:
   - 납 브릿지(40%), 부품 미삽(25%) 등 주요 결함이 전체 불량의 65% 이상을 차지함을 명확하게 시각화.
4. **합격 기준**: 파레토 누적 백분율 계산 오차 0.1% 이하 및 CSV 정상 다운로드.

---

### 🔹 Phase 10: 스마트폰 무선 브리지 & 독립형 패키징 (`TC-INT-10`)
1. **사전 조건**: 모바일 기기(스마트폰) 준비 또는 세션 브라우저 탭 분리.
2. **실행 절차**:
   - 상단 헤더 **[📱 스마트폰 연결]** 모달 실행 ➔ QR 코드 스캔 또는 세션 URL 진입.
   - 스마트폰 화면에서 모바일 전용 카메라 송출 노드(`MobileDevNode`) 기동.
   - 메인 PC에서 스마트폰 카메라 스트림 실시간 수신 확인.
   - 좌측 패널 하단의 **[단독 실행형 HTML 패키징]** 버튼 클릭.
3. **기대 결과**:
   - 외부 서버 없이 로컬 단독으로 구동되는 독립형 `기구물_비전_검사_v3.html` 파일이 즉시 생성되어 다운로드됨.
4. **합격 기준**: 무선 스트림 15fps 이상 수신 및 다운로드된 단독 HTML 파일의 오프라인 100% 정상 작동.

---

## 📦 시나리오 참조 데이터셋 (Scenario Reference Dataset)

### 1. 결함 유형별 참조 테스트 샘플 정의
| 결함 ID | 결함 유형 (Defect Type) | 심각도 (Severity) | 결함 설명 및 발생 위치 | 기대 판정 | 유사도 범위 |
| :--- | :--- | :---: | :--- | :---: | :---: |
| `DEF-01` | **납 브릿지 (Solder Bridge)** | `CRITICAL` | U1 MCU QFP-64 핀 간격 사이 솔더 볼 및 브릿지 쇼트 | **NG** | 70% ~ 76% |
| `DEF-02` | **부품 미삽 (Missing Part)** | `CRITICAL` | R1 칩 저항 미실장으로 동박 패드만 노출 | **NG** | 65% ~ 70% |
| `DEF-03` | **부품 오삽/틀어짐 (Misaligned)** | `MAJOR` | C2 탄탈 커패시터 30도 각도 비틀림 실장 | **NG** | 80% ~ 84% |
| `DEF-04` | **표면 스크래치 (Scratch)** | `MINOR` | 솔더레지스트 표면 12mm 선형 긁힘흔 | **NG** | 85% ~ 90% |
| `DEF-05` | **하우징 크랙 (Crack)** | `CRITICAL` | 기구물 외곽 체결 홀 주변 4.5mm 관통 크랙 | **NG** | 73% ~ 78% |
| `DEF-06` | **이물질 오염 (Contamination)** | `MINOR` | 전원 레귤레이터 IC 주변 플럭스 잔여물 점착 | **NG** | 87% ~ 91% |
| `DEF-OK` | **정상 마스터 (Golden Sample)** | `NORMAL` | 전 부품 정상 실장, 오염/손상 없음 | **OK** | 98% ~ 100% |

### 2. 바코드 / QR 코드 시나리오 매핑 테이블
| 시리얼 번호 (Serial) | 바코드 값 (Barcode) | 제품 유형 | 할당 테스트 시나리오 |
| :--- | :--- | :--- | :--- |
| `SN-2026-PCB-001` ~ `SN-2026-PCB-003` | `AOI-PCB-M01` ~ `AOI-PCB-M03` | ARM 메인보드 Rev 2.4 | 정상 양품 연속 검사 (OK) |
| `SN-2026-PCB-004` | `AOI-PCB-M04` | ARM 메인보드 Rev 2.4 | `DEF-01` 납 브릿지 결함 검사 (NG) |
| `SN-2026-PCB-007` | `AOI-PCB-M07` | ARM 메인보드 Rev 2.4 | `DEF-02` 부품 미삽 결함 검사 (NG) |
| `SN-2026-PCB-012` | `AOI-PCB-M12` | ARM 메인보드 Rev 2.4 | `DEF-03` 부품 오삽 결함 검사 (NG) |
| `SN-2026-PCB-015` | `AOI-PCB-M15` | ARM 메인보드 Rev 2.4 | `DEF-04` 표면 스크래치 검사 (NG) |
| `SN-2026-PCB-018` | `AOI-PCB-M18` | 기구물 하우징 모듈 | `DEF-05` 하우징 크랙 검사 (NG) |
| `SN-2026-PCB-021` | `AOI-PCB-M21` | 전원 보드 모듈 | `DEF-06` 플럭스 이물 검사 (NG) |

---

## 🚀 1-클릭 시나리오 데이터셋 로드 방법 (UI 연동)

1. 상단 메뉴의 **[🧠 불량 지식베이스 & AI 학습 센터]** 모달을 엽니다.
2. 우측 상단의 **[🧪 시나리오 테스트 데이터셋 로드]** 버튼을 클릭합니다.
3. 사전 정의된 10건의 표준 결함 레코드와 20건의 검사 이력이 즉시 로드되며, 파레토 분석 대시보드와 AI 자가학습 기능을 실시간으로 즉시 관통 검증할 수 있습니다.
